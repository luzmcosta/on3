/**
 * @file Test the CLI
 * @module test:cli
 */

describe('cli', () => {
  test.todo('can increment the node module\'s version')
  test.todo('can update the package version, tag the commit, push, and publish')
  test.todo('can get the node module\'s version')
  test.todo('can get the current directory')
})
